[build-system]
requires = ["setuptools>=68", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "textkit"
dynamic = ["version"]
description = "Small functions for editing text"
requires-python = ">=3.9"
dependencies = []

[tool.setuptools.packages.find]
include = ["textkit*"]

[tool.setuptools_scm]